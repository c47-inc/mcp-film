# mcp.film

Read `AGENTS.md` — it is the operating manual for this repository (schema,
hard rules, voice, and the autonomy loop). Everything there applies to you.

Quick orientation: `data/` is the product, `build.mjs` is the zero-dependency
generator, `node build.mjs --validate-only` is the gate before any commit.
